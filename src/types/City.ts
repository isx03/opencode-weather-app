export interface City {
  name: string;
  country?: string;
  latitude: number;
  longitude: number;
}

export interface GeoResult {
  name: string;
  country?: string;
  latitude: number;
  longitude: number;
}